class msg {
    constructor(msg = '', color = '', inv = '') {
        this.msg = msg;
        this.color = color;
        this.inv = inv;
    }
    setMsg(msg, color, inv) {
        this.msg = msg;
        this.color = color;
        this.inv = inv;
    }
}
//# sourceMappingURL=msg.js.map